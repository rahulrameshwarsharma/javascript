const gameName = new String ('ShivSundarSharma');
console.log (gameName);

// always use baktics "`" for writting the strings. because using "+" to concatinate 
// is not a good way, so we must avoid that in mordern days.

// Go through MDN and write a detailed article on strings.


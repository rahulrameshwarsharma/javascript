// ******************************* Numbers *****************************

const score = 100;
console.log(score);

const Balance = new Number(400);
console.log(Balance);


// This is used in ecommerce type websites, to show customers pricise value

const otherNumber = 123.565632
console.log(otherNumber.toFixed(2));    // used for number of values need after decimal

console.log(otherNumber.toPrecision(5));    /* used for number of total values need including decimal,
                                                also it rages between 1 - 21 charactors
                                                "toprecision" method always returns String.
                                            */